= form_for @spam do |f|
  - if @spam.errors.any?
    #error_explanation
      %h2= "#{pluralize(@spam.errors.count, "error")} prohibited this spam from being saved:"
      %ul
        - @spam.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
